const React = require('react');

const Home = () => {
    return (
        <div>
            <h1>Welcome!</h1>
            <p>You are now logged in.</p>
        </div>
    );
};


export default Home;